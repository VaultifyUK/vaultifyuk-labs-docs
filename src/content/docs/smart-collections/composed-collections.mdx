---
title: Composed Collections
description: Build a collection by aggregating products from multiple other Smart Collections–managed collections, with optional filtering on top.
sidebar:
  order: 6
---

import { Steps, LinkCard, CardGrid } from '@astrojs/starlight/components';

A composed collection draws its products from one or more other managed collections — called **source collections** — rather than from a rule that runs directly against your full catalogue. This is useful when you want to combine the membership of several targeted collections into one place, or reuse an existing collection's rules without duplicating them.

![Composed collection detail page showing two source collections and a local filter rule with the live product count](/screenshots/placeholder.png)

## How composed evaluation works

When a composed collection is evaluated, Smart Collections:

<Steps>

1. **Collect source products** — gathers all products currently in each active source collection
2. **Union** — combines them into a single pool; duplicates from overlapping sources are removed automatically
3. **Filter (if applicable)** — if you have added local rule conditions to the composed collection, filters the pool to only products that satisfy those conditions
4. **Sort** — sorts the final set according to the collection's sort configuration

</Steps>

The composed collection then reflects exactly this set of products in Shopify.

### Automatic cascade updates

Whenever a source collection changes — because a product matched or stopped matching its rules — Smart Collections automatically re-evaluates all composed collections that use it as a source. You do not need to trigger this manually.

:::tip
Composed collections work well for aggregator collections. If you have separate brand or category collections and want a single "All Products" collection, compose it from all the individual collections. Any future changes to the source collections propagate automatically.
:::

## Managing sources

### Add a source collection

Only managed collections (those already owned by Smart Collections) can be added as sources.

<Steps>

1. Open the composed collection in Smart Collections
2. In the **Sources** section, click **Add source**
3. Select a managed collection from the candidate list
4. Save — a sync runs immediately to incorporate the new source

</Steps>

Adding the first source to a rule-based collection converts it to a composed collection. See [Converting back to rule-based](#converting-back-to-rule-based) if you need to reverse this.

### Remove a source collection

<Steps>

1. Open the composed collection in Smart Collections
2. In the **Sources** section, find the source to remove and click **Remove**
3. Confirm — a sync runs to update membership without that source's products

</Steps>

You cannot remove the last remaining source. To remove all sources, use **Convert to rule-based** instead.

### Convert back to rule-based

<Steps>

1. Open the composed collection
2. Click **Convert to rule-based** in the Sources section
3. Confirm in the dialog

</Steps>

This removes all composition links. The collection is then evaluated using only its local rule conditions (if any). If no local conditions are configured, the collection will be empty after the next sync.

:::caution
Converting to rule-based removes all source links. This cannot be automatically undone. You can re-add sources afterwards if needed.
:::

## Adding local rule conditions

You can add rule conditions directly to a composed collection. These filter the union of source products — only products that are in at least one source collection **and** satisfy the local conditions are included.

Local rules follow the same format as [Rule-Based Collections](/smart-collections/rule-based-collections/). They cannot add products that are not already in a source; they can only narrow the set.

:::tip
A composed collection with no local conditions simply reflects the full union of its sources. Add local conditions only when you need to narrow that result.
:::

## Disabled sources

If a source collection is disabled (paused), it stops contributing products. The composition link is preserved, so re-enabling the source restores its contribution on the next sync.

A source that is released or deleted is automatically unlinked from all composed collections it fed.

## Composition health

The collection detail page shows any issues detected with source relationships. Issues are surfaced with severity levels — critical issues prevent the composition from producing a useful result; warnings indicate a degraded but still-functional state.

### Per-collection health issues

| Issue | Severity | Meaning | What to do |
| --- | --- | --- | --- |
| No active sources | Critical | All sources are disabled — the composition is contributing no products | Re-enable at least one source collection. Navigate to the source and click **Reactivate automation**. |
| Source disabled | Warning | One or more sources are paused — their products are not being contributed | Re-enable the source, or remove it if no longer needed |
| Source sync failed | Warning | A source encountered an error on its last sync — its product set may be stale | Open the source collection and click **Sync now**; investigate via the Sync Dashboard if failures persist |
| Source never synced | Warning | A source was added but has not completed its first sync yet | Wait for the source's initial sync to complete; if stuck, trigger a sync on the source collection |
| Source stale | Warning | A source's last successful sync completed more than 24 hours ago | Open the source collection and check its sync status; trigger a manual sync if needed |
| Source empty | Note | A source has active syncs but currently has no matching products — it contributes nothing | Review the source collection's rules; if intentional, no action needed |

![Composed collection detail page showing a source health warning with severity indicator](/screenshots/placeholder.png)

### Shop-wide composition graph issues

In addition to per-collection health, the app validates the overall composition dependency graph across your store. Issues detected at the graph level are surfaced on the home dashboard as attention items.

| Issue | Severity | Meaning | What to do |
| --- | --- | --- | --- |
| Cycle detected | Critical | Two or more composed collections reference each other as sources, creating a circular dependency | Remove the link that creates the cycle; compositions must form a directed acyclic graph |
| Duplicate link | Warning | A source collection is linked to a composed collection more than once | Remove the duplicate source link from the composed collection |
| Deleted source link | Warning | A source collection that was deleted still appears as a source | Open the composed collection and remove the broken source link |

:::caution
A composition cycle prevents evaluation from completing for all collections involved. If you see a cycle warning on the home dashboard, resolve it immediately by removing one of the source links that forms the loop.
:::

Composition health issues appear on the [Overview Dashboard](/smart-collections/overview-dashboard/) and link directly to the affected collections.

Raw diagnostic codes are not shown in the UI. This section describes the conditions they represent.

## Terminology

Use these terms consistently when working with composition. Avoid "parent" and "child" — those terms are reserved for collection hierarchy, which is a separate feature.

| Term | Meaning |
| --- | --- |
| **Source collection** | A managed collection that contributes its products to a composed collection |
| **Composed collection** | A collection that draws its membership from one or more source collections |

## Pins and sort order

Composed collections respect [Product Pins](/smart-collections/product-pins/) and [Sorting & Automation](/smart-collections/sorting-and-automation/) in the same way as rule-based collections. Pin positions survive re-sorts and sync cycles regardless of which source a product came from.

## Related topics

<CardGrid>
  <LinkCard title="Rule-Based Collections" href="/smart-collections/rule-based-collections/" description="Define membership with direct product conditions." />
  <LinkCard title="Product Pins" href="/smart-collections/product-pins/" description="Hold products at fixed positions in any collection." />
  <LinkCard title="Sorting & Automation" href="/smart-collections/sorting-and-automation/" description="Control product ordering and automatic sync." />
  <LinkCard title="Troubleshooting" href="/smart-collections/troubleshooting/" description="Diagnose empty or unexpected composed collections." />
</CardGrid>
